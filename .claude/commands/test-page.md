---
description: Generate a test HTML page for a specific conversion scenario
argument-hint: [scenario - e.g. "nested tables", "code blocks", "heavy navigation"]
---
Create a minimal test HTML page that exercises: $ARGUMENTS

The page should be self-contained and loadable locally in Chrome for testing the DeMarkup extension. Include enough realistic content to verify the conversion handles the scenario correctly.
