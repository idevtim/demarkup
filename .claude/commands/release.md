---
description: Package and release the extension to GitHub
---
## Release Extension

Run the full release pipeline (package ZIP, tag, GitHub release):

!`./scripts/release.sh 2>&1 | tail -20`

Report whether the release succeeded. If it failed, show the relevant error.
